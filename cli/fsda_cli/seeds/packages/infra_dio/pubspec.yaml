name: infra_dio
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.10.7

dependencies:
  app_core:
    path: ../app_core
  

dev_dependencies:
